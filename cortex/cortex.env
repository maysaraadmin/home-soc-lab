# Cortex Configuration
# Copy this file to .env and update the values

# Core Secrets - CHANGE THESE IN PRODUCTION
CORTEX_SECRET=change_this_to_a_secure_secret
CORTEX_CRYPTO_SECRET=change_this_to_a_secure_crypto_secret

# Database Configuration
POSTGRES_PASSWORD=change_this_password
POSTGRES_USER=cortex
POSTGRES_DB=cortex

# Redis Configuration
REDIS_PASSWORD=

# Proxy Configuration (if needed)
http_proxy=
https_proxy=
no_proxy=localhost,127.0.0.1,elasticsearch,redis,postgres
